# ~/.gitignore

# Dependency directories
# -------------------------------------------------------------------
# These folders contain all the packages your project needs to run.
# They are installed automatically via 'npm install' and should never be committed.
node_modules/
pages/
# Build output
# -------------------------------------------------------------------
# This is the folder where the final, optimized version of your app is placed
# after running 'npm run build'. It's generated from your source code.
dist/
build/

# Vite-specific cache
# -------------------------------------------------------------------
# Vite uses this directory to cache dependencies and speed up the development server.
.vite/

# Local environment variables and secrets
# -------------------------------------------------------------------
# CRITICAL: Never commit files containing secrets like API keys or client secrets.
# It's best practice to put them in a .env file and add that file here.
.env
.env.local
.env.*.local

# Log files
# -------------------------------------------------------------------
# Log files can contain sensitive information and are specific to your local machine.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE and editor directories
# -------------------------------------------------------------------
# These folders store your personal editor settings and should not be shared.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Operating System files
# -------------------------------------------------------------------
# Files generated automatically by macOS, Windows, and Linux.
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Test reports and coverage
# -------------------------------------------------------------------
# Output from running tests, which is generated each time tests are run.
coverage/
.nyc_output/

.env
